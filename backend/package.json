{
    "name": "backend",
    "version": "0.0.0",
    "description": "",
    "scripts": {
        "start": "node dist/backend/src/index.js",
        "setup": "ts-patch install",
        "build": "tsc && tsc-alias",
        "build:watch": "tsc && (concurrently \"tsc -w\" \"tsc-alias -w\")",
        "clean": "rm -rf dist",
        "test": "tsc && tsc-alias && uvu dist/backend/tests",
        "lint": "biome check",
        "lint:fix": "biome check --write"
    },
    "author": "",
    "license": "UNLICENSED",
    "devDependencies": {
        "@biomejs/biome": "1.9.4",
        "@types/node": "^22.14.0",
        "concurrently": "^9.1.2",
        "ts-patch": "^3.3.0",
        "ts-runtime-checks": "^0.6.3",
        "tsc-alias": "^1.8.13",
        "typescript": "^5.8.3",
        "uvu": "^0.5.6"
    }
}
